import * as RadixColors from '@radix-ui/colors';

export const lightTheme = {
  textPrimary: RadixColors.gray.gray12,
  textSecondary: RadixColors.gray.gray11,
  background: RadixColors.gray.gray1,
  // Add more colors as needed
};

export const darkTheme = {
  textPrimary: RadixColors.gray.gray1,
  textSecondary: RadixColors.gray.gray2,
  background: RadixColors.gray.gray12,
  // Add more colors as needed
};